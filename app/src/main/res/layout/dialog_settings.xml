<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_glass_panel"
    android:orientation="vertical"
    android:paddingStart="61dp"
    android:paddingTop="80dp"
    android:paddingEnd="61dp"
    android:paddingBottom="48dp">

    <TextView
        android:id="@+id/tvSettingsSectionTitle"
        style="@style/AppGolosText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Настройки"
        android:textColor="#FFFFFF"
        android:textSize="16sp" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="20dp"
        android:fillViewport="true"
        android:scrollbars="none">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <TextView android:id="@+id/btnPlaylistSettings" style="@style/SettingsCard" android:text="Настроить плейлисты" android:drawableStart="@android:drawable/ic_menu_sort_by_size"/>
            <TextView android:id="@+id/btnEpgSelect" style="@style/SettingsCard" android:layout_marginTop="16dp" android:text="Настройка программы передач (EPG)" android:drawableStart="@android:drawable/ic_menu_my_calendar"/>
            <TextView android:id="@+id/btnSleepTimerSettings" style="@style/SettingsCard" android:layout_marginTop="16dp" android:text="Таймер сна" android:drawableStart="@android:drawable/ic_lock_idle_alarm"/>

            <LinearLayout
                style="@style/SettingsCard"
                android:layout_marginTop="16dp"
                android:orientation="horizontal"
                android:gravity="center_vertical">
                <TextView android:layout_width="0dp" android:layout_height="wrap_content" android:layout_weight="1" android:text="Выбор страницы при открытии приложения" android:textColor="#FFFFFF" android:textSize="16sp"/>
                <ToggleButton
                    android:id="@+id/tbStartMode"
                    android:layout_width="wrap_content"
                    android:layout_height="36dp"
                    android:textOn="Последний канал"
                    android:textOff="Стартовая страница"
                    android:textAllCaps="false"
                    android:textSize="12sp"
                    android:minWidth="180dp" />
            </LinearLayout>

            <TextView android:id="@+id/btnExtraSettings" style="@style/SettingsCard" android:layout_marginTop="16dp" android:text="Дополнительные настройки" android:drawableStart="@android:drawable/ic_menu_manage"/>
            <TextView android:id="@+id/btnUserSettings" style="@style/SettingsCard" android:layout_marginTop="16dp" android:text="Настроить пользователя" android:drawableStart="@android:drawable/ic_menu_myplaces"/>
        </LinearLayout>
    </ScrollView>

    <TextView
        android:id="@+id/btnCloseSettingsDialog"
        style="@style/WhiteTitleTextShadow"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:background="@drawable/bg_watch_button"
        android:gravity="center"
        android:padding="10dp"
        android:text="Закрыть"
        android:textColor="#FFFFFF" />

</LinearLayout>
